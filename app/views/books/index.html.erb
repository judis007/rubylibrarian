 <center><h1>Books</h1></center>
   
 
 <table class="table container table-bordered">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Author</th>
      <th scope="col">Synopsis</th>
      <th scope="col" colspan = "4">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
    <tr>
      <td><%= book.name %></td>
      <td><%= book.author %></td>
      <td><%= book.synopsis %></td>
      <td><%= link_to "Delete Book", book_path(book), method: :delete, class:"btn btn-danger", data: {confirm: "Are you sure?"} %></td>
      <td><%= link_to "Show", book_path(book), class:"btn btn-warning" %></td>
      <td><%= link_to "Edit", edit_book_path(book), class: "btn btn-success" %></td>
      <td></td>

      </tr>
    <% end %>
   </tbody>
   </table>

   <div class = "container">
  <%= link_to "Add New Book", new_book_path(@books), class: "btn btn-info" %>
   </div>
   
  
   
